[project]
name = "perfect-wedding"
version = "0.1.0"
description = "Wedding app — info, chat, AI assistant, toastmaster tools"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
