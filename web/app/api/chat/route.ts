import { NextResponse } from "next/server";

export async function POST() {
  // Placeholder — will be connected to Claude API
  return NextResponse.json({
    message: "AI assistant endpoint — not yet implemented",
  });
}
